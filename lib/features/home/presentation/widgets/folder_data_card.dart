import 'package:flutter/cupertino.dart';

class FolderDataCard extends StatelessWidget {
  const FolderDataCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
